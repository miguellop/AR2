<!-- Workspace copilot instructions (auto-created) -->

- Verify project type and language with the user if unsure. This scaffold uses a Python hello-world by default.
- Use `src/` for source and `tests/` for unit tests.
- Before making language-specific changes, confirm the target language/framework.
